# Live TS CLI captures (generated by scripts/generate-ts-goldens.sh).
# These contain real API responses and are for manual review only.
# The checked-in *.ts.golden.jsonl files are hand-crafted test artifacts.
*.ts.capture.jsonl
